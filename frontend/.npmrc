; Silence npm's run-script echo (the "> foolsboard@x dev" / "> vite" lines) so the
; dev terminal shows only the foolsboard banner. Vite's own output (banner, URLs,
; build summary, errors) is unaffected -- it's the child process's stdout/stderr.
loglevel=silent
