/// <reference types="vite/client" />

// Build version, injected by Vite's `define` (see vite.config.ts).
declare const __APP_VERSION__: string
// Raw CHANGELOG.md contents, injected at build time (see vite.config.ts).
declare const __CHANGELOG__: string
