# Core backend dependencies (DB-agnostic; SQLite works with zero extra installs)
fastapi>=0.111
uvicorn[standard]>=0.30
sqlalchemy>=2.0
alembic>=1.13
pydantic-settings>=2.2
python-multipart>=0.0.9   # required for file uploads
pillow>=10                # image (re)compression on upload (video/audio use ffmpeg)
